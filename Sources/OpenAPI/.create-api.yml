generate: [entities, paths, enums]
module: AppStoreConnect_Swift_SDK
indentation: tabs
useNaiveDate: false
fileHeaderComment: |
  // Generated by Create API
  // https://github.com/CreateAPI/CreateAPI
  //
  // swiftlint:disable all
paths:
  imports: [] # Prevent importing Get since we define our own Request type
  namespace: APIEndpoint
  overriddenBodyTypes:
    application/a-gzip: Data
    application/vnd.apple.xcode-metrics+json: Data
    application/vnd.apple.diagnostic-logs+json: Data
entities:
  includeIdentifiableConformance: true
  defaultType: struct
  typeOverrides:
    MetricCategory: finalClass
